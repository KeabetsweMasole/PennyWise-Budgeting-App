package com.example.navigation

import org.junit.Test

import org.junit.Assert.*

/* This file contains simple tests to make sure basic math and logic are working correctly in the app code */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        /* This is just a basic test to see if 2 plus 2 equals 4, confirming tests can run */
        assertEquals(4, 2 + 2)
    }
}